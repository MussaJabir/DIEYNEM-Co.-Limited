# WeasyPrint 62.x crashes with pydyf >= 0.11 (Kozea/WeasyPrint#2620).
# 69.0 is the current maintained release and requires pydyf >= 0.11; both are
# pinned together so the pairing can't drift again.
weasyprint==69.0
pydyf==0.12.1
Jinja2==3.1.4
